import Posts from "../Posts/Posts";
import { Container } from "react-bootstrap";

const Home = () => {
    return (
        <Container>
            <Posts/>
        </Container>
    );
};

export default Home;