import Posts from "../Post/Post";
import { Container } from "react-bootstrap";

const Home = () => {
    return (
        <Container>
            <Posts/>
        </Container>
    );
};

export default Home;