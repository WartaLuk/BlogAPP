import NavBar from "../NavBar/NavBar";
import { Container } from "react-bootstrap";

const Header = () => {
    return (
        <Container>
            <NavBar/>
        </Container>
    );
};
export default Header;